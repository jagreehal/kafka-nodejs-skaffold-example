{
  "name": "@jag/kafka-consumer1",
  "version": "1.0.0",
  "description": "Kafka consumer",
  "main": "dist/index.js",
  "scripts": {
    "build": "rimraf dist && tsc",
    "dev": "ts-node-dev --no-notify --transpile-only --respawn -- src/index.ts",
    "start": "NODE_ENV=production node dist/index.js"
  },
  "dependencies": {
    "express": "^4.18.1",
    "kafkajs": "^2.0.0"
  },
  "devDependencies": {
    "@types/node": "^17.0.31",
    "rimraf": "^3.0.2",
    "ts-node-dev": "^1.1.8",
    "typescript": "^4.6.4"
  }
}
